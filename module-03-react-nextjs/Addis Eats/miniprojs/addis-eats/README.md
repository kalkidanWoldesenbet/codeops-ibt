# Addis Eats

A static Addis Eats menu built with React and Vite.

## What It Is

This project demonstrates the basics of React components, JSX, props, and rendering an array of dishes using `map()` and unique keys.

The menu contains reusable `Dish` components and a `Header` component.
